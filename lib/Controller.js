var Class = require('./Class');

/**
 * A Controller handles requests and sends responses.
 */
module.exports = Class.extend({

	init: function init() {
		// TODO: Determine what should be in a controller vs extending the http.ClientRequest and http.ServerResponse
	}

});